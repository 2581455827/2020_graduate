	首先，zigbee节点通过下位机程序将采集到的数据（温湿度，气体浓度，光强）整合为数据包，通过无线网络发送给zigbee协调器，协调器收到数据包后，通过串口将数据
传输给PC，PC端通过TCP协议，将数据发送至WEB端，WEB端对数据进行解析，并实时显示于界面上并将数据存储于数据库中，完成一次数据的采集与显示，该过程一秒钟执行一次。
	其中，每次zigbee节点整合为数据包后，都会去判断各个传感器的值是否在正常范围内，例如，如果光强不在设定范围内，则自动开启LED等，气体浓度过大，自动开启风扇